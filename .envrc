# Keep optional human or machine settings local to this checkout. Agent worktrees
# do not copy this ignored file.
source_env_if_exists .envrc.local

# Anchor uv and direct tool invocations to this worktree's own environment.
export UV_PROJECT_ENVIRONMENT="$PWD/.venv"
PATH_add "$UV_PROJECT_ENVIRONMENT/bin"
